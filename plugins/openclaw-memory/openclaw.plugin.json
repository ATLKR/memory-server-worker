{
  "id": "allenlim-memory",
  "name": "Allenlim Memory",
  "description": "Cloudflare Agent Memory as OpenClaw's primary memory slot.",
  "kind": "memory",
  "activation": {
    "onStartup": true
  },
  "contracts": {
    "tools": [
      "memory_search",
      "memory_get"
    ]
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "serverUrl": {
        "type": "string"
      },
      "application": {
        "type": "string",
        "minLength": 1,
        "maxLength": 100
      },
      "credentialCommand": {
        "type": "string",
        "minLength": 1
      },
      "credentialArgs": {
        "type": "array",
        "items": {
          "type": "string"
        }
      },
      "requestTimeoutMs": {
        "type": "integer",
        "minimum": 100,
        "maximum": 120000
      },
      "autoRecall": {
        "type": "boolean"
      },
      "autoCapture": {
        "type": "boolean"
      }
    },
    "required": ["credentialCommand"]
  }
}
