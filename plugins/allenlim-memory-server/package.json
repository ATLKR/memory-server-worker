{
  "name": "allenlim-memory-server",
  "version": "3.1.0",
  "description": "Personal persistent memory tools for Codex and Claude Code, plus reusable Devin skills. Uses the remote memory-server-worker MCP endpoint.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "bin": {
    "mem": "scripts/cli.mjs",
    "memory-mcp-stdio": "scripts/mcp-stdio.mjs"
  },
  "scripts": {
    "pre-prompt": "node scripts/pre-prompt.mjs",
    "post-turn": "node scripts/post-turn.mjs",
    "mem": "node scripts/cli.mjs",
    "test": "node --test scripts/*.test.mjs",
    "typecheck": "node --check scripts/lib.mjs && node --check scripts/cli.mjs && node --check scripts/mcp-stdio.mjs && node --check scripts/hook-runner.mjs && node --check scripts/pre-prompt.mjs && node --check scripts/post-turn.mjs"
  }
}
